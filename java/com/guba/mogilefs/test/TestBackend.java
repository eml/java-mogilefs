/*
 * Created on Jun 27, 2005
 *
 * copyright ill.com 2005
 */
package com.guba.mogilefs.test;

/**
 * @author ericlambrecht
 *  
 */
public class TestBackend {
    /*
     * private static Logger log = Logger.getLogger(TestBackend.class);
     * 
     * public static void main(String[] args) { BasicConfigurator.configure();
     * 
     * try { Backend backend = new Backend(new String[] {"qbert.guba.com:7001"},
     * true);
     * 
     * log.debug("constructed and connected to qbert ok"); } catch
     * (BadHostFormatException e) { log.error("bad host format exception", e); }
     * catch (NoTrackersException e) { log.error("no trackers exception", e); }
     * 
     * try { Backend backend = new Backend(new String[] {"qbert.guba.com:7001"},
     * true); Map response = backend.doRequest("ECHO", new String[] { "eric",
     * "r00lez" });
     * 
     * log.debug("constructed qbert connection ok, sent command, and received
     * error response: " + backend.getLastErrStr()); } catch
     * (BadHostFormatException e) { log.error("bad host format exception", e); }
     * catch (NoTrackersException e) { log.error("no trackers exception", e); }
     * catch (TrackerCommunicationException e) { log.error("tracker comm
     * exception", e); } }
     */
}
